package Controllers;

/**
 * La clase SnakeGame es la clase principal que inicia el juego de la serpiente.
 */
public class SnakeGame {
	/**
	 * El método main es el punto de entrada del programa.
	 * Crea una nueva instancia del controlador del juego y comienza el juego.
	 *
	 * @param args Los argumentos de la línea de comandos. No se utilizan en este programa.
	 */
	public static void main(String[] args) {
		// Crear una nueva instancia del controlador del juego
		GameController snakeGame = new GameController();
		// Aquí es donde comenzaría el juego
	}
}
